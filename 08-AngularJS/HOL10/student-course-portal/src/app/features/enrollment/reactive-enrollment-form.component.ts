import { Component } from '@angular/core'; import { AsyncPipe, NgFor, NgIf } from '@angular/common'; import { AbstractControl, FormArray, FormBuilder, ReactiveFormsModule, ValidationErrors, Validators } from '@angular/forms';
function noCourseCode(control: AbstractControl): ValidationErrors | null { return String(control.value ?? '').startsWith('XX') ? { noCourseCode: true } : null; }
function simulateEmailCheck(control: AbstractControl): Promise<ValidationErrors | null> { return new Promise(resolve => setTimeout(() => resolve(String(control.value ?? '').includes('test@') ? { emailTaken: true } : null), 800)); }
@Component({ standalone: true, imports: [ReactiveFormsModule, NgFor, NgIf, AsyncPipe], template: `<h1>Reactive Enrollment</h1><form [formGroup]="enrollForm" (ngSubmit)="submit()"><label>Name<input formControlName="studentName"></label><label>Email<input formControlName="studentEmail"></label><span class="error" *ngIf="enrollForm.controls.studentEmail.errors?.['emailTaken']">Email taken.</span><label>Course ID/code<input formControlName="courseId"></label><span class="error" *ngIf="enrollForm.controls.courseId.errors?.['noCourseCode']">Course code starting with XX is not allowed.</span><label>Semester<select formControlName="preferredSemester"><option>Odd</option><option>Even</option></select></label><label><input type="checkbox" formControlName="agreeToTerms"> I agree to terms</label><section formArrayName="additionalCourses"><h3>Additional courses</h3><div *ngFor="let ctrl of additionalCourses.controls; let i=index"><input [formControl]="ctrl"><button type="button" (click)="removeCourse(i)">Remove</button></div><button type="button" (click)="addCourse()">Add Another Course</button></section><p><button [disabled]="enrollForm.invalid">Submit</button></p></form>` })
export class ReactiveEnrollmentFormComponent { readonly enrollForm = this.fb.group({ studentName: ['', [Validators.required, Validators.minLength(3)]], studentEmail: this.fb.control('', [Validators.required, Validators.email], [simulateEmailCheck]), courseId: ['', [Validators.required, noCourseCode]], preferredSemester: ['Odd', Validators.required], agreeToTerms: [false, Validators.requiredTrue], additionalCourses: this.fb.array([]) }); constructor(private fb: FormBuilder) {} get additionalCourses() { return this.enrollForm.controls.additionalCourses; } addCourse() { this.additionalCourses.push(this.fb.control('', Validators.required)); } removeCourse(index: number) { this.additionalCourses.removeAt(index); } submit() { console.log(this.enrollForm.value, this.enrollForm.getRawValue()); /* value omits disabled controls; getRawValue includes them. */ } }
