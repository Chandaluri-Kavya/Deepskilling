import { Injectable } from '@angular/core'; import { Course } from '../models/course.model';
@Injectable({ providedIn: 'root' }) export class EnrollmentService { private enrolledCourseIds: number[] = []; enroll(id: number) { if (!this.enrolledCourseIds.includes(id)) this.enrolledCourseIds.push(id); } unenroll(id: number) { this.enrolledCourseIds = this.enrolledCourseIds.filter(x => x !== id); } isEnrolled(id: number) { return this.enrolledCourseIds.includes(id); } getEnrolledIds() { return [...this.enrolledCourseIds]; } getEnrolledCourses(courses: Course[]) { return courses.filter(c => this.isEnrolled(c.id)); } }
