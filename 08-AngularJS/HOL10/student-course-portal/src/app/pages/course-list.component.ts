import { Component, OnInit } from '@angular/core'; import { AsyncPipe, NgFor, NgIf } from '@angular/common'; import { ActivatedRoute, Router } from '@angular/router'; import { FormsModule } from '@angular/forms'; import { Store } from '@ngrx/store'; import { CourseCardComponent } from '../components/course-card.component'; import { Course } from '../models/course.model'; import { loadCourses } from '../store/course/course.actions'; import { selectAllCourses, selectCoursesError, selectCoursesLoading } from '../store/course/course.selectors'; import { enrollInCourse, unenrollFromCourse } from '../store/enrollment/enrollment.actions'; import { selectEnrolledIds } from '../store/enrollment/enrollment.selectors';
@Component({ standalone: true, imports: [AsyncPipe, NgFor, NgIf, FormsModule, CourseCardComponent], template: `<label>Search <input [(ngModel)]="searchTerm" (ngModelChange)="updateSearch()"></label><p *ngIf="loading$ | async">Loading courses...</p><p class="error" *ngIf="error$ | async as error">{{ error }}</p><section *ngIf="!(loading$ | async)"><ng-container *ngIf="courses$ | async as courses"><div *ngIf="courses.length; else noCourses"><app-course-card *ngFor="let course of courses; trackBy: trackByCourseId" [course]="course" [enrolled]="(enrolledIds$ | async)?.includes(course.id) ?? false" (enrollRequested)="onEnroll($event)"></app-course-card></div><ng-template #noCourses><p>No courses available.</p></ng-template></ng-container></section>` })
export class CourseListComponent implements OnInit { courses$ = this.store.select(selectAllCourses); loading$ = this.store.select(selectCoursesLoading); error$ = this.store.select(selectCoursesError); enrolledIds$ = this.store.select(selectEnrolledIds); searchTerm = ''; constructor(private store: Store, private router: Router, private route: ActivatedRoute) {} ngOnInit() { this.searchTerm = this.route.snapshot.queryParamMap.get('search') ?? ''; this.store.dispatch(loadCourses()); } trackByCourseId(_: number, course: Course) { return course.id; } updateSearch() { this.router.navigate(['/courses'], { queryParams: { search: this.searchTerm || null } }); } onEnroll(courseId: number) { this.store.dispatch(enrollInCourse({ courseId })); } }
