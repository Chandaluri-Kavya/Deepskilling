# HOL03 — Directives & Pipes

Corresponds to PDF Hands-On 3 (tasks 25–37). The cumulative runnable solution is in [`../HOL10/student-course-portal`](../HOL10/student-course-portal).

Features delivered: loading and empty states, `ngFor` with `trackBy`, `ngSwitch`, `ngClass`, `ngStyle`, configurable `appHighlight`, and the `creditLabel` pipe.
