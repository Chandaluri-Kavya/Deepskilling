import { TestBed } from '@angular/core/testing'; import { provideHttpClient } from '@angular/common/http'; import { HttpTestingController, provideHttpClientTesting } from '@angular/common/http/testing'; import { CourseService } from './course.service'; import { Course } from '../models/course.model';
describe('CourseService', () => { let service: CourseService; let http: HttpTestingController; const courses: Course[] = [{ id: 1, name: 'Data Structures', code: 'CS101', credits: 4, gradeStatus: 'passed' }, { id: 2, name: 'Angular', code: 'NG101', credits: 3, gradeStatus: 'pending' }]; beforeEach(() => { TestBed.configureTestingModule({ providers: [CourseService, provideHttpClient(), provideHttpClientTesting()] }); service = TestBed.inject(CourseService); http = TestBed.inject(HttpTestingController); }); afterEach(() => http.verify()); it('gets courses from the expected URL', () => { service.getCourses().subscribe(result => expect(result.length).toBe(2)); const request = http.expectOne('http://localhost:3000/courses'); request.flush(courses); }); it('returns a friendly error after a failed request', () => { service.getCourses().subscribe({ error: error => expect(error.message).toBe('Failed to load courses. Please try again.') }); for (let retry = 0; retry < 3; retry++) http.expectOne('http://localhost:3000/courses').flush({}, { status: 500, statusText: 'Server error' }); }); });
